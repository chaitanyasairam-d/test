from setuptools import setup, find_packages


VERSION = '0.0.1'
DESCRIPTION = 'A sample python package'
LONG_DESCRIPTION = 'a sample description'

# Setting up
setup(
    name="myPackage",
    version=VERSION,
    author="chaitanya",
    packages=find_packages()
)
